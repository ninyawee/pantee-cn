import maplibregl from 'maplibre-gl';
type $$ComponentProps = {
    scheme: string;
    loadFn: maplibregl.AddProtocolAction;
};
declare const Protocol: import("svelte").Component<$$ComponentProps, {}, "">;
type Protocol = ReturnType<typeof Protocol>;
export default Protocol;
