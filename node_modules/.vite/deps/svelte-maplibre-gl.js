import {
  require_maplibre_gl
} from "./chunk-JYR7GWBA.js";
import "./chunk-MZVN5SDE.js";
import "./chunk-SSMPYKFB.js";
import {
  add_locations,
  assign,
  bind_this,
  check_target,
  clsx,
  create_ownership_validator,
  each,
  hmr,
  if_block,
  init,
  legacy_api,
  log_if_contains_state,
  onDestroy,
  prop,
  rest_props,
  set_class,
  set_style,
  snippet,
  spread_props,
  wrap_snippet
} from "./chunk-XXFA4PRF.js";
import "./chunk-U7P2NEEE.js";
import {
  append,
  comment,
  from_html
} from "./chunk-KQD4U7I7.js";
import {
  FILENAME,
  HMR,
  add_svelte_meta,
  child,
  first_child,
  get,
  getContext,
  noop,
  pop,
  proxy,
  push,
  reset,
  set,
  setContext,
  sibling,
  snapshot,
  state,
  strict_equals,
  tag,
  tag_proxy,
  template_effect,
  track_reactivity_loss,
  untrack,
  update,
  user_derived,
  user_effect
} from "./chunk-GUOWMT4K.js";
import "./chunk-VZQZA5WB.js";
import "./chunk-OHYQYV5R.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/svelte-maplibre-gl/dist/global/Protocol.svelte
var import_maplibre_gl = __toESM(require_maplibre_gl());
Protocol[FILENAME] = "node_modules/svelte-maplibre-gl/dist/global/Protocol.svelte";
function Protocol($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Protocol);
  user_effect(() => {
    import_maplibre_gl.default.addProtocol($$props.scheme, $$props.loadFn);
    const prevName = $$props.scheme;
    return () => {
      import_maplibre_gl.default.removeProtocol(prevName);
    };
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Protocol = hmr(Protocol);
  import.meta.hot.accept((module) => {
    Protocol[HMR].update(module.default);
  });
}
var Protocol_default = Protocol;

// node_modules/svelte-maplibre-gl/dist/map/MapLibre.svelte
var import_maplibre_gl3 = __toESM(require_maplibre_gl());

// node_modules/svelte-maplibre-gl/dist/contexts.svelte.js
var MAP_CONTEXT_KEY = /* @__PURE__ */ Symbol("MapLibre map context");
var SOURCE_CONTEXT_KEY = /* @__PURE__ */ Symbol("MapLibre source context");
var LAYER_CONTEXT_KEY = /* @__PURE__ */ Symbol("MapLibre layer context");
var MARKER_CONTEXT_KEY = /* @__PURE__ */ Symbol("MapLibre marker context");
var MapContext = class {
  #_map = tag(
    /** Map instance */
    state(null),
    "MapContext._map"
  );
  get _map() {
    return get(this.#_map);
  }
  set _map(value) {
    set(this.#_map, value);
  }
  _listener = void 0;
  _pending = [];
  userLayers = /* @__PURE__ */ new Set();
  userSources = /* @__PURE__ */ new Set();
  baseTerrain;
  userTerrain;
  baseSky;
  userSky;
  baseLight;
  userLight;
  baseProjection;
  userProjection;
  get map() {
    return this._map;
  }
  set map(value) {
    if (this._listener) {
      this._map?.off("styledata", this._listener);
      this._listener = void 0;
    }
    this._map = value;
    if (this._map) {
      this._listener = this._onstyledata.bind(this);
      this._map.on("styledata", this._listener);
    }
  }
  addLayer(addLayerObject, beforeId) {
    if (!this.map) throw new Error("Map is not initialized");
    this.userLayers.add(addLayerObject.id);
    this.waitForStyleLoaded((map) => {
      map.addLayer(addLayerObject, beforeId);
    });
  }
  removeLayer(id) {
    if (!this.map) throw new Error("Map is not initialized");
    this.userLayers.delete(id);
    this.waitForStyleLoaded((map) => {
      map.removeLayer(id);
    });
  }
  addSource(id, source) {
    this.userSources.add(id);
    this.waitForStyleLoaded((map) => {
      map.addSource(id, source);
    });
  }
  removeSource(id) {
    this.userSources.delete(id);
    this.waitForStyleLoaded((map) => {
      map.removeSource(id);
    });
  }
  /** Wait for the style to be loaded before calling the function */
  waitForStyleLoaded(func) {
    if (!this.map) {
      return;
    }
    if (this.map.style._loaded) {
      func(this.map);
    } else {
      this._pending.push(func);
    }
  }
  _onstyledata(ev) {
    const map = ev.target;
    if (map?.style._loaded) {
      for (const func of this._pending) {
        func(map);
      }
      this._pending = [];
    }
  }
  setStyle(style) {
    const { userSources: addedSources, userLayers: addedLayers } = this;
    if (!style) {
      this.map?.setStyle(null);
      return;
    }
    this.map?.setStyle(snapshot(style), {
      // Preserves user styles when the base style changes
      transformStyle: (previous, next) => {
        this.baseLight = next.light;
        this.baseProjection = next.projection;
        this.baseSky = next.sky;
        this.baseTerrain = next.terrain;
        if (!previous) {
          return next;
        }
        const sources = next.sources;
        for (const [key, value] of Object.entries(previous.sources)) {
          if (addedSources.has(key)) {
            sources[key] = value;
          }
        }
        const userLayers = previous.layers.filter((layer) => addedLayers.has(layer.id));
        const layers = [...next.layers, ...userLayers];
        return {
          ...next,
          light: this.userLight || this.baseLight,
          projection: this.userProjection || this.baseProjection,
          sky: this.userSky || this.baseSky,
          terrain: this.userTerrain || this.baseTerrain,
          sources,
          layers
        };
      }
    });
  }
};
function prepareMapContext() {
  const mapCtx = new MapContext();
  setContext(MAP_CONTEXT_KEY, mapCtx);
  return mapCtx;
}
function getMapContext() {
  const mapCtx = getContext(MAP_CONTEXT_KEY);
  if (!mapCtx) throw new Error("Map context not found");
  return mapCtx;
}
var SourceContext = class {
  #id = tag(
    /** sourceId */
    state(""),
    "SourceContext.id"
  );
  get id() {
    return get(this.#id);
  }
  set id(value) {
    set(this.#id, value, true);
  }
};
function prepareSourceContext() {
  const sourceCtx = new SourceContext();
  setContext(SOURCE_CONTEXT_KEY, sourceCtx);
  return sourceCtx;
}
function getSourceContext() {
  const sourceCtx = getContext(SOURCE_CONTEXT_KEY);
  if (!sourceCtx || !sourceCtx.id) throw new Error("Source context not found");
  return sourceCtx;
}
var LayerContext = class {
  #id = tag(state(""), "LayerContext.id");
  get id() {
    return get(this.#id);
  }
  set id(value) {
    set(this.#id, value, true);
  }
};
function prepareLayerContext() {
  const layerCtx = new LayerContext();
  setContext(LAYER_CONTEXT_KEY, layerCtx);
  return layerCtx;
}
function getLayerContext() {
  const layerCtx = getContext(LAYER_CONTEXT_KEY);
  if (!layerCtx || !layerCtx.id) throw new Error("Layer context not found");
  return layerCtx;
}
var MarkerContext = class {
  #marker = tag(state(null), "MarkerContext.marker");
  get marker() {
    return get(this.#marker);
  }
  set marker(value) {
    set(this.#marker, value);
  }
};
function prepareMarkerContext() {
  const markerCtx = new MarkerContext();
  setContext(MARKER_CONTEXT_KEY, markerCtx);
  return markerCtx;
}
function getMarkerContext() {
  const markerCtx = getContext(MARKER_CONTEXT_KEY);
  if (!markerCtx) {
    return null;
  }
  return markerCtx;
}

// node_modules/svelte-maplibre-gl/dist/utils.js
var import_maplibre_gl2 = __toESM(require_maplibre_gl(), 1);
var layerIdCounter = 0;
var sourceIdCounter = 0;
function generateLayerID() {
  return `svmlgl-layer-${layerIdCounter++}`;
}
function generateSourceID() {
  return `svmlgl-source-${sourceIdCounter++}`;
}
function resetEventListener(evented, type, listener) {
  if (listener) {
    evented?.on(type, listener);
  }
  const prevListener = listener;
  return () => {
    if (prevListener) {
      evented?.off(type, prevListener);
    }
  };
}
function resetLayerEventListener(map, type, layer, listener) {
  if (listener) {
    map?.on(type, layer, listener);
  }
  const prevListener = listener;
  return () => {
    if (prevListener) {
      map?.off(type, layer, prevListener);
    }
  };
}
function formatLngLat(target, lnglat) {
  if (Array.isArray(target)) {
    return [lnglat.lng, lnglat.lat];
  } else if ("lon" in target) {
    return { lon: lnglat.lng, lat: lnglat.lat };
  } else {
    return { lng: lnglat.lng, lat: lnglat.lat };
  }
}

// node_modules/svelte-maplibre-gl/dist/map/MapLibre.svelte
MapLibre[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/MapLibre.svelte";
var root = add_locations(from_html(`<div><!></div>`), MapLibre[FILENAME], [[601, 0]]);
function MapLibre($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, MapLibre);
  var $$ownership_validator = create_ownership_validator($$props);
  let map = prop($$props, "map", 15, void 0), className = prop($$props, "class", 3, ""), inlineStyle = prop($$props, "inlineStyle", 3, ""), autoloadGlobalCss = prop($$props, "autoloadGlobalCss", 3, true), padding = prop($$props, "padding", 19, () => ({ top: 0, bottom: 0, left: 0, right: 0 })), bearing = prop($$props, "bearing", 15, void 0), center = prop($$props, "center", 15, void 0), elevation = prop($$props, "elevation", 15, void 0), interactive = prop($$props, "interactive", 3, void 0), pitch = prop($$props, "pitch", 15, void 0), roll = prop($$props, "roll", 15, void 0), style = prop($$props, "style", 19, () => ({ version: 8, sources: {}, layers: [] })), zoom = prop($$props, "zoom", 15, void 0), globalState = prop($$props, "globalState", 19, () => ({})), restOptions = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "map",
      "class",
      "inlineStyle",
      "children",
      "autoloadGlobalCss",
      "onerror",
      "onload",
      "onidle",
      "onremove",
      "onrender",
      "onresize",
      "onwebglcontextlost",
      "onwebglcontextrestored",
      "ondataloading",
      "ondata",
      "ontiledataloading",
      "onsourcedataloading",
      "onstyledataloading",
      "onsourcedata",
      "onstyledata",
      "onstyleimagemissing",
      "ondataabort",
      "onsourcedataabort",
      "onboxzoomcancel",
      "onboxzoomstart",
      "onboxzoomend",
      "ontouchcancel",
      "ontouchmove",
      "ontouchend",
      "ontouchstart",
      "onclick",
      "oncontextmenu",
      "ondblclick",
      "onmousemove",
      "onmouseup",
      "onmousedown",
      "onmouseout",
      "onmouseover",
      "onmovestart",
      "onmove",
      "onmoveend",
      "onzoomstart",
      "onzoom",
      "onzoomend",
      "onrotatestart",
      "onrotate",
      "onrotateend",
      "ondragstart",
      "ondrag",
      "ondragend",
      "onpitchstart",
      "onpitch",
      "onpitchend",
      "onwheel",
      "onterrain",
      "oncooperativegestureprevented",
      "onprojectiontransition",
      "padding",
      "fov",
      "cursor",
      "showTileBoundaries",
      "showPadding",
      "showCollisionBoxes",
      "showOverdrawInspector",
      "repaint",
      "vertices",
      "bearing",
      "bearingSnap",
      "center",
      "centerClampedToGround",
      "elevation",
      "interactive",
      "maxBounds",
      "maxPitch",
      "maxZoom",
      "minPitch",
      "minZoom",
      "pitch",
      "pixelRatio",
      "renderWorldCopies",
      "roll",
      "style",
      "transformRequest",
      "zoom",
      "boxZoom",
      "cancelPendingTileRequestsWhileZooming",
      "cooperativeGestures",
      "doubleClickZoom",
      "dragPan",
      "dragRotate",
      "keyboard",
      "scrollZoom",
      "touchPitch",
      "touchZoomRotate",
      "transformCameraUpdate",
      "globalState"
    ],
    "restOptions"
  );
  if (autoloadGlobalCss() && globalThis.window && !document.querySelector('link[href$="/maplibre-gl.css"]')) {
    const link = document.createElement("link");
    link.rel = "stylesheet";
    link.href = `https://unpkg.com/maplibre-gl@${import_maplibre_gl3.default.getVersion()}/dist/maplibre-gl.css`;
    document.head.appendChild(link);
  }
  let container = tag(state(void 0), "container");
  const mapCtx = prepareMapContext();
  user_effect(() => {
    if (map() || !get(container)) {
      return;
    }
    const options = {
      container: get(container),
      ...Object.fromEntries(Object.entries({
        // Map Options (reactive)
        bearing: bearing(),
        bearingSnap: $$props.bearingSnap,
        center: center(),
        centerClampedToGround: $$props.centerClampedToGround,
        elevation: elevation(),
        interactive: interactive(),
        maxBounds: $$props.maxBounds,
        maxPitch: $$props.maxPitch,
        maxZoom: $$props.maxZoom,
        minPitch: $$props.minPitch,
        minZoom: $$props.minZoom,
        pitch: pitch(),
        pixelRatio: $$props.pixelRatio,
        renderWorldCopies: $$props.renderWorldCopies,
        roll: roll(),
        style: style(),
        transformRequest: $$props.transformRequest,
        zoom: zoom(),
        // Map Options (Map properties)
        boxZoom: $$props.boxZoom,
        cancelPendingTileRequestsWhileZooming: $$props.cancelPendingTileRequestsWhileZooming,
        cooperativeGestures: $$props.cooperativeGestures,
        doubleClickZoom: $$props.doubleClickZoom,
        dragPan: $$props.dragPan,
        dragRotate: $$props.dragRotate,
        keyboard: $$props.keyboard,
        scrollZoom: $$props.scrollZoom,
        touchPitch: $$props.touchPitch,
        touchZoomRotate: $$props.touchZoomRotate,
        transformCameraUpdate: $$props.transformCameraUpdate,
        // Map Options (others)
        ...restOptions
        // filter out undefined values
      }).filter(([, v]) => strict_equals(v, void 0, false)))
    };
    map(new import_maplibre_gl3.default.Map(options));
    mapCtx.map = map() ?? null;
    if (strict_equals(padding(), void 0, false)) {
      map().setPadding(padding());
    }
    if ($$props.cursor) {
      map().getCanvas().style.cursor = $$props.cursor ?? "";
    }
    map().on("move", () => {
      if (!map()) {
        return;
      }
      const tr = map().transform;
      if (center()) {
        const _center = import_maplibre_gl3.default.LngLat.convert(center());
        if (strict_equals(_center.lat, tr.center.lat, false) || strict_equals(_center.lng, tr.center.lng, false)) {
          center(formatLngLat(center(), tr.center));
        }
      } else {
        center(tr.center);
      }
      if (strict_equals(tr.zoom, zoom(), false)) {
        zoom(tr.zoom);
      }
      if (strict_equals(tr.bearing, bearing(), false)) {
        bearing(tr.bearing);
      }
      if (strict_equals(tr.pitch, pitch(), false)) {
        pitch(tr.pitch);
      }
      if (strict_equals(tr.roll, roll(), false)) {
        roll(tr.roll);
      }
      if (strict_equals(tr.elevation, elevation(), false)) {
        elevation(tr.elevation);
      }
    });
  });
  let prevGlobalStateKeys = [];
  user_effect(() => {
    if (!mapCtx.map) {
      return;
    }
    const newKeys = new Set(Object.keys(globalState()));
    mapCtx.waitForStyleLoaded((map2) => {
      for (const key of prevGlobalStateKeys) {
        if (!newKeys.has(key)) {
          map2.setGlobalStateProperty(key, null);
        }
      }
      for (const key of newKeys) {
        map2.setGlobalStateProperty(key, globalState()[key]);
      }
    });
    prevGlobalStateKeys = Array.from(newKeys);
  });
  user_effect(() => resetEventListener(map(), "boxzoomcancel", $$props.onboxzoomcancel));
  user_effect(() => resetEventListener(map(), "boxzoomend", $$props.onboxzoomend));
  user_effect(() => resetEventListener(map(), "boxzoomstart", $$props.onboxzoomstart));
  user_effect(() => resetEventListener(map(), "click", $$props.onclick));
  user_effect(() => resetEventListener(map(), "contextmenu", $$props.oncontextmenu));
  user_effect(() => resetEventListener(map(), "cooperativegestureprevented", $$props.oncooperativegestureprevented));
  user_effect(() => resetEventListener(map(), "data", $$props.ondata));
  user_effect(() => resetEventListener(map(), "dataabort", $$props.ondataabort));
  user_effect(() => resetEventListener(map(), "dataloading", $$props.ondataloading));
  user_effect(() => resetEventListener(map(), "dblclick", $$props.ondblclick));
  user_effect(() => resetEventListener(map(), "drag", $$props.ondrag));
  user_effect(() => resetEventListener(map(), "dragend", $$props.ondragend));
  user_effect(() => resetEventListener(map(), "dragstart", $$props.ondragstart));
  user_effect(() => resetEventListener(map(), "error", $$props.onerror));
  user_effect(() => resetEventListener(map(), "idle", $$props.onidle));
  user_effect(() => resetEventListener(map(), "load", $$props.onload));
  user_effect(() => resetEventListener(map(), "mousedown", $$props.onmousedown));
  user_effect(() => resetEventListener(map(), "mousemove", $$props.onmousemove));
  user_effect(() => resetEventListener(map(), "mouseout", $$props.onmouseout));
  user_effect(() => resetEventListener(map(), "mouseover", $$props.onmouseover));
  user_effect(() => resetEventListener(map(), "mouseup", $$props.onmouseup));
  user_effect(() => resetEventListener(map(), "move", $$props.onmove));
  user_effect(() => resetEventListener(map(), "moveend", $$props.onmoveend));
  user_effect(() => resetEventListener(map(), "movestart", $$props.onmovestart));
  user_effect(() => resetEventListener(map(), "pitch", $$props.onpitch));
  user_effect(() => resetEventListener(map(), "pitchend", $$props.onpitchend));
  user_effect(() => resetEventListener(map(), "pitchstart", $$props.onpitchstart));
  user_effect(() => resetEventListener(map(), "projectiontransition", $$props.onprojectiontransition));
  user_effect(() => resetEventListener(map(), "remove", $$props.onremove));
  user_effect(() => resetEventListener(map(), "render", $$props.onrender));
  user_effect(() => resetEventListener(map(), "resize", $$props.onresize));
  user_effect(() => resetEventListener(map(), "rotate", $$props.onrotate));
  user_effect(() => resetEventListener(map(), "rotateend", $$props.onrotateend));
  user_effect(() => resetEventListener(map(), "rotatestart", $$props.onrotatestart));
  user_effect(() => resetEventListener(map(), "sourcedata", $$props.onsourcedata));
  user_effect(() => resetEventListener(map(), "sourcedataabort", $$props.onsourcedataabort));
  user_effect(() => resetEventListener(map(), "sourcedataloading", $$props.onsourcedataloading));
  user_effect(() => resetEventListener(map(), "styledata", $$props.onstyledata));
  user_effect(() => resetEventListener(map(), "styledataloading", $$props.onstyledataloading));
  user_effect(() => resetEventListener(map(), "styleimagemissing", $$props.onstyleimagemissing));
  user_effect(() => resetEventListener(map(), "terrain", $$props.onterrain));
  user_effect(() => resetEventListener(map(), "tiledataloading", $$props.ontiledataloading));
  user_effect(() => resetEventListener(map(), "touchcancel", $$props.ontouchcancel));
  user_effect(() => resetEventListener(map(), "touchend", $$props.ontouchend));
  user_effect(() => resetEventListener(map(), "touchmove", $$props.ontouchmove));
  user_effect(() => resetEventListener(map(), "touchstart", $$props.ontouchstart));
  user_effect(() => resetEventListener(map(), "webglcontextlost", $$props.onwebglcontextlost));
  user_effect(() => resetEventListener(map(), "webglcontextrestored", $$props.onwebglcontextrestored));
  user_effect(() => resetEventListener(map(), "wheel", $$props.onwheel));
  user_effect(() => resetEventListener(map(), "zoom", $$props.onzoom));
  user_effect(() => resetEventListener(map(), "zoomend", $$props.onzoomend));
  user_effect(() => resetEventListener(map(), "zoomstart", $$props.onzoomstart));
  let firstRun = true;
  user_effect(() => {
    className();
    const classNames = (className() ?? "")?.split(/\s/).filter(Boolean);
    if (get(container) && !firstRun) {
      for (const className2 of classNames) {
        get(container).classList.add(className2);
      }
    }
    return () => {
      if (get(container)) {
        for (const className2 of classNames) {
          get(container).classList.remove(className2);
        }
      }
    };
  });
  user_effect(() => {
    if (strict_equals($$props.fov, void 0, false)) {
      map()?.setVerticalFieldOfView($$props.fov);
    }
  });
  user_effect(() => {
    $$props.cursor;
    if (map() && !firstRun) {
      map().getCanvas().style.cursor = $$props.cursor ?? "";
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.showTileBoundaries, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "showTileBoundaries"], map(map().showTileBoundaries = $$props.showTileBoundaries, true), 376, 3);
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.showPadding, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "showPadding"], map(map().showPadding = $$props.showPadding, true), 381, 3);
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.showCollisionBoxes, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "showCollisionBoxes"], map(map().showCollisionBoxes = $$props.showCollisionBoxes, true), 386, 3);
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.showOverdrawInspector, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "showOverdrawInspector"], map(map().showOverdrawInspector = $$props.showOverdrawInspector, true), 391, 3);
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.repaint, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "repaint"], map(map().repaint = $$props.repaint, true), 396, 3);
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.vertices, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "vertices"], map(map().vertices = $$props.vertices, true), 401, 3);
    }
  });
  user_effect(() => {
    center();
    zoom();
    bearing();
    pitch();
    roll();
    elevation();
    padding();
    if (!firstRun && map()) {
      let notAlmostEqual = function(a, b) {
        return Math.abs(a - b) > 1e-14;
      };
      const tr = map()._getTransformForUpdate();
      let jumpTo = {};
      let changed = false;
      if (center()) {
        const _center = import_maplibre_gl3.default.LngLat.convert(center());
        if (notAlmostEqual(tr.center.lat, _center.lat) || notAlmostEqual(tr.center.lng, _center.lng)) {
          jumpTo.center = center();
          changed = true;
        }
      }
      if (strict_equals(zoom(), void 0, false) && notAlmostEqual(tr.zoom, zoom())) {
        jumpTo.zoom = zoom();
        changed = true;
      }
      if (strict_equals(bearing(), void 0, false) && notAlmostEqual(tr.bearing, bearing())) {
        jumpTo.bearing = bearing();
        changed = true;
      }
      if (strict_equals(pitch(), void 0, false) && strict_equals(tr.pitch, pitch(), false)) {
        jumpTo.pitch = pitch();
        changed = true;
      }
      if (strict_equals(roll(), void 0, false) && strict_equals(tr.roll, roll(), false)) {
        jumpTo.roll = roll();
        changed = true;
      }
      if (strict_equals(elevation(), void 0, false) && strict_equals(tr.elevation, elevation(), false)) {
        jumpTo.elevation = elevation();
        changed = true;
      }
      if (padding() && !tr.isPaddingEqual(padding())) {
        jumpTo.padding = padding();
        changed = true;
      }
      if (changed) {
        const originalStop = map().stop;
        $$ownership_validator.mutation("map", ["map", "stop"], map(map().stop = () => map()._stop(true), true), 459, 4);
        map()?.jumpTo(jumpTo, { reactivity: true });
        $$ownership_validator.mutation("map", ["map", "stop"], map(map().stop = originalStop, true), 461, 4);
      }
    }
  });
  user_effect(() => {
    $$props.bearingSnap;
    if (map() && $$props.bearingSnap && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "_bearingSnap"], map(map()._bearingSnap = $$props.bearingSnap, true), 468, 3);
    }
  });
  user_effect(() => {
    $$props.centerClampedToGround;
    if (!firstRun) {
      map()?.setCenterClampedToGround($$props.centerClampedToGround ?? false);
    }
  });
  user_effect(() => {
    $$props.maxBounds;
    if (!firstRun) {
      map()?.setMaxBounds($$props.maxBounds);
    }
  });
  user_effect(() => {
    $$props.maxPitch;
    if (!firstRun) {
      map()?.setMaxPitch($$props.maxPitch);
    }
  });
  user_effect(() => {
    $$props.maxZoom;
    if (!firstRun) {
      map()?.setMaxZoom($$props.maxZoom);
    }
  });
  user_effect(() => {
    $$props.minPitch;
    if (!firstRun) {
      map()?.setMinPitch($$props.minPitch);
    }
  });
  user_effect(() => {
    $$props.minZoom;
    if (!firstRun) {
      map()?.setMinZoom($$props.minZoom);
    }
  });
  user_effect(() => {
    $$props.pixelRatio;
    if (!firstRun) {
      map()?.setPixelRatio($$props.pixelRatio ?? null);
    }
  });
  user_effect(() => {
    $$props.renderWorldCopies;
    if (!firstRun) {
      map()?.setRenderWorldCopies($$props.renderWorldCopies ?? null);
    }
  });
  user_effect(() => {
    style();
    if (!firstRun) {
      mapCtx.setStyle(style());
    }
  });
  user_effect(() => {
    $$props.transformRequest;
    if (!firstRun) {
      map()?.setTransformRequest($$props.transformRequest);
    }
  });
  user_effect(() => {
    if (strict_equals($$props.boxZoom, void 0, false) && !firstRun) {
      $$props.boxZoom ? map()?.boxZoom.enable() : map()?.boxZoom.disable();
    }
  });
  user_effect(() => {
    if (map() && strict_equals($$props.cancelPendingTileRequestsWhileZooming, void 0, false) && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "cancelPendingTileRequestsWhileZooming"], map(map().cancelPendingTileRequestsWhileZooming = $$props.cancelPendingTileRequestsWhileZooming, true), 540, 3);
    }
  });
  user_effect(() => {
    if (strict_equals($$props.cooperativeGestures, void 0, false) && !firstRun) {
      $$props.cooperativeGestures ? map()?.cooperativeGestures.enable() : map()?.cooperativeGestures.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.doubleClickZoom, void 0, false) && !firstRun) {
      $$props.doubleClickZoom ? map()?.doubleClickZoom.enable() : map()?.doubleClickZoom.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.dragPan, void 0, false) && !firstRun) {
      $$props.dragPan ? map()?.dragPan.enable($$props.dragPan) : map()?.dragPan.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.dragRotate, void 0, false) && !firstRun) {
      $$props.dragRotate ? map()?.dragRotate.enable() : map()?.dragRotate.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.keyboard, void 0, false) && !firstRun) {
      $$props.keyboard ? map()?.keyboard.enable() : map()?.keyboard.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.scrollZoom, void 0, false) && !firstRun) {
      $$props.scrollZoom ? map()?.scrollZoom.enable($$props.scrollZoom) : map()?.scrollZoom.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.touchPitch, void 0, false) && !firstRun) {
      $$props.touchPitch ? map()?.touchPitch.enable($$props.touchPitch) : map()?.touchPitch.disable();
    }
  });
  user_effect(() => {
    if (strict_equals($$props.touchZoomRotate, void 0, false) && !firstRun) {
      $$props.touchZoomRotate ? map()?.touchZoomRotate.enable($$props.touchZoomRotate) : map()?.touchZoomRotate.disable();
    }
  });
  user_effect(() => {
    $$props.transformCameraUpdate;
    if (map() && !firstRun) {
      $$ownership_validator.mutation("map", ["map", "transformCameraUpdate"], map(map().transformCameraUpdate = $$props.transformCameraUpdate ?? null, true), 586, 3);
    }
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    mapCtx.map = null;
    map()?.remove();
    map(void 0);
  });
  var $$exports = { ...legacy_api() };
  var div = root();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var fragment = comment();
      var node_1 = first_child(fragment);
      add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop, map), "render", MapLibre, 603, 2);
      append($$anchor2, fragment);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (map()) $$render(consequent);
      }),
      "if",
      MapLibre,
      602,
      1
    );
  }
  reset(div);
  bind_this(div, ($$value) => set(container, $$value), () => get(container));
  template_effect(() => {
    set_class(div, 1, clsx(className()));
    set_style(div, inlineStyle());
  });
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  MapLibre = hmr(MapLibre);
  import.meta.hot.accept((module) => {
    MapLibre[HMR].update(module.default);
  });
}
var MapLibre_default = MapLibre;

// node_modules/svelte-maplibre-gl/dist/map/Sky.svelte
var import_maplibre_gl4 = __toESM(require_maplibre_gl());
Sky[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Sky.svelte";
function Sky($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Sky);
  let spec = rest_props($$props, ["$$slots", "$$events", "$$legacy"], "spec");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  user_effect(() => {
    mapCtx.userSky = snapshot(spec);
    mapCtx.waitForStyleLoaded((map) => {
      map.setSky(mapCtx.userSky);
    });
  });
  onDestroy(() => {
    mapCtx.userSky = void 0;
    mapCtx.waitForStyleLoaded((map) => {
      map.setSky(mapCtx.baseSky);
    });
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Sky = hmr(Sky);
  import.meta.hot.accept((module) => {
    Sky[HMR].update(module.default);
  });
}
var Sky_default = Sky;

// node_modules/svelte-maplibre-gl/dist/map/Light.svelte
var import_maplibre_gl5 = __toESM(require_maplibre_gl());
Light[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Light.svelte";
function Light($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Light);
  let spec = rest_props($$props, ["$$slots", "$$events", "$$legacy"], "spec");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  user_effect(() => {
    mapCtx.userLight = snapshot(spec);
    mapCtx.waitForStyleLoaded((map) => {
      map.setLight(mapCtx.userLight);
    });
  });
  onDestroy(() => {
    mapCtx.userLight = void 0;
    mapCtx.waitForStyleLoaded((map) => {
      map.setLight(mapCtx.baseLight ?? {});
    });
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Light = hmr(Light);
  import.meta.hot.accept((module) => {
    Light[HMR].update(module.default);
  });
}
var Light_default = Light;

// node_modules/svelte-maplibre-gl/dist/map/Projection.svelte
var import_maplibre_gl6 = __toESM(require_maplibre_gl());
Projection[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Projection.svelte";
function Projection($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Projection);
  let spec = rest_props($$props, ["$$slots", "$$events", "$$legacy"], "spec");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  user_effect(() => {
    mapCtx.userProjection = snapshot(spec);
    if (!mapCtx.userProjection.type) {
      mapCtx.userProjection.type = "mercator";
    }
    mapCtx.waitForStyleLoaded((map) => {
      map.setProjection(mapCtx.userProjection);
    });
  });
  onDestroy(() => {
    mapCtx.userProjection = void 0;
    mapCtx.waitForStyleLoaded((map) => {
      map.setProjection(mapCtx.baseProjection ?? { type: "mercator" });
    });
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Projection = hmr(Projection);
  import.meta.hot.accept((module) => {
    Projection[HMR].update(module.default);
  });
}
var Projection_default = Projection;

// node_modules/svelte-maplibre-gl/dist/map/Terrain.svelte
var import_maplibre_gl7 = __toESM(require_maplibre_gl());
Terrain[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Terrain.svelte";
function Terrain($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Terrain);
  let spec = rest_props($$props, ["$$slots", "$$events", "$$legacy", "source"], "spec");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  const sourceId = tag(user_derived(() => $$props.source ?? getSourceContext().id), "sourceId");
  user_effect(() => {
    mapCtx.userTerrain = snapshot({ ...spec, source: get(sourceId) });
    mapCtx.waitForStyleLoaded((map) => {
      map.setTerrain(mapCtx.userTerrain || null);
    });
  });
  onDestroy(() => {
    mapCtx.userTerrain = void 0;
    mapCtx.waitForStyleLoaded((map) => {
      map.setTerrain(mapCtx.baseTerrain ?? null);
    });
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Terrain = hmr(Terrain);
  import.meta.hot.accept((module) => {
    Terrain[HMR].update(module.default);
  });
}
var Terrain_default = Terrain;

// node_modules/svelte-maplibre-gl/dist/map/Image.svelte
var import_maplibre_gl8 = __toESM(require_maplibre_gl());
Image[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Image.svelte";
function Image($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Image);
  const mapCtx = getMapContext();
  let prevId = $$props.id;
  let firstRun = true;
  user_effect(() => {
    if (!mapCtx.map) {
      return;
    }
    let prevImage = mapCtx.map.getImage(prevId);
    if (prevImage && (strict_equals($$props.id, prevId, false) || strict_equals($$props.image.width, prevImage.data.width, false) || strict_equals($$props.image.height, prevImage.data.height, false))) {
      mapCtx.map?.removeImage(prevId);
      prevId = $$props.id;
    }
    if (!prevImage) {
      mapCtx.map.addImage($$props.id, $$props.image, untrack(() => $$props.options));
      firstRun = true;
    } else {
      mapCtx.map.updateImage($$props.id, $$props.image);
    }
  });
  user_effect(() => {
    $$props.options;
    if (!firstRun) {
      const image = mapCtx.map?.getImage($$props.id);
      if (!image) {
        return;
      }
      image.pixelRatio = $$props.options?.pixelRatio ?? 1;
      image.sdf = $$props.options?.sdf ?? false;
      image.stretchX = $$props.options?.stretchX;
      image.stretchY = $$props.options?.stretchY;
      image.content = $$props.options?.content;
      image.textFitWidth = $$props.options?.textFitWidth;
      image.textFitHeight = $$props.options?.textFitHeight;
      mapCtx.map?.style.updateImage($$props.id, image);
    }
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    mapCtx.map?.removeImage($$props.id);
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Image = hmr(Image);
  import.meta.hot.accept((module) => {
    Image[HMR].update(module.default);
  });
}
var Image_default = Image;

// node_modules/svelte-maplibre-gl/dist/map/Sprite.svelte
Sprite[FILENAME] = "node_modules/svelte-maplibre-gl/dist/map/Sprite.svelte";
function Sprite($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Sprite);
  const mapCtx = getMapContext();
  user_effect(() => {
    if (!mapCtx.map) {
      return;
    }
    mapCtx.waitForStyleLoaded((map) => {
      map.addSprite($$props.id, $$props.url);
    });
    return () => {
      mapCtx.waitForStyleLoaded((map) => {
        map.removeSprite($$props.id);
      });
    };
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  Sprite = hmr(Sprite);
  import.meta.hot.accept((module) => {
    Sprite[HMR].update(module.default);
  });
}
var Sprite_default = Sprite;

// node_modules/svelte-maplibre-gl/dist/sources/VectorTileSource.svelte
var import_maplibre_gl10 = __toESM(require_maplibre_gl());

// node_modules/svelte-maplibre-gl/dist/sources/RawSource.svelte
var import_maplibre_gl9 = __toESM(require_maplibre_gl());
RawSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/RawSource.svelte";
function RawSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RawSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  spec = spec;
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let firstRun = true;
  const id = $$props.id ?? generateSourceID();
  const sourceCtx = prepareSourceContext();
  sourceCtx.id = id;
  mapCtx.waitForStyleLoaded((map) => {
    mapCtx.addSource(id, snapshot(spec));
    source(map.getSource(id));
    firstRun = true;
  });
  user_effect(() => {
    if (source() && strict_equals($$props.type, "canvas", false) && strict_equals($$props.type, "video", false) && strict_equals($$props.type, "image", false)) {
      $$ownership_validator.mutation("source", ["source", "maxzoom"], source(source().maxzoom = $$props.maxzoom ?? 22, true), 43, 3);
      if (strict_equals($$props.type, "geojson", false)) {
        $$ownership_validator.mutation("source", ["source", "minzoom"], source(source().minzoom = $$props.minzoom ?? 0, true), 45, 4);
      }
    }
  });
  user_effect(() => {
    if (source() && (strict_equals($$props.type, "vector") || strict_equals($$props.type, "raster") || strict_equals($$props.type, "raster-dem"))) {
      $$props.tiles;
      if (!firstRun && "setTiles" in source()) {
        source().setTiles($$props.tiles ?? []);
      }
    }
  });
  user_effect(() => {
    if (source() && (strict_equals($$props.type, "vector") || strict_equals($$props.type, "raster") || strict_equals($$props.type, "raster-dem"))) {
      $$props.url;
      if (!firstRun && "setUrl" in source()) {
        source().setUrl($$props.url);
      }
    }
  });
  user_effect(() => {
    if (source() && strict_equals($$props.type, "image")) {
      $$props.url;
      if (!firstRun) {
        source().updateImage({ url: $$props.url });
      }
    }
  });
  user_effect(() => {
    if (source() && (strict_equals($$props.type, "image") || strict_equals($$props.type, "video") || strict_equals($$props.type, "canvas"))) {
      $$props.coordinates;
      if (!firstRun) {
        if (!("setCoordinates" in source())) throw new Error("setCoordinates method is not supported");
        source().setCoordinates($$props.coordinates);
      }
    }
  });
  user_effect(() => {
    if (strict_equals($$props.type, "canvas")) {
      $$props.animate;
      if (source() && strict_equals($$props.animate, void 0, false) && !firstRun) {
        const cs = source();
        $$props.animate ? cs.play() : cs.pause();
      }
    }
  });
  user_effect(() => {
    if (source() && strict_equals($$props.type, "geojson")) {
      $$props.data;
      if (!firstRun) {
        source().setData($$props.data);
      }
    }
  });
  user_effect(() => {
    if (source() && strict_equals($$props.type, "geojson")) {
      $$props.cluster;
      $$props.clusterMaxZoom;
      $$props.clusterRadius;
      if (!firstRun) {
        if (strict_equals($$props.clusterRadius, void 0, false)) {
          $$ownership_validator.mutation("source", ["source", "workerOptions", "superclusterOptions", "radius"], source(source().workerOptions.superclusterOptions.radius = $$props.clusterRadius * (8192 / source().tileSize), true), 107, 6);
        }
        source().setClusterOptions({
          cluster: $$props.cluster,
          clusterMaxZoom: $$props.clusterMaxZoom
          // clusterRadius: spec.clusterRadius, // TODO: Requires a fix in maplibre-gl-js
        });
      }
    }
  });
  user_effect(() => {
    source();
    firstRun = false;
  });
  onDestroy(() => {
    mapCtx.removeSource(id);
    source(void 0);
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(() => snippet(node, () => $$props.children ?? noop), "render", RawSource, 130, 0);
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  RawSource = hmr(RawSource);
  import.meta.hot.accept((module) => {
    RawSource[HMR].update(module.default);
  });
}
var RawSource_default = RawSource;

// node_modules/svelte-maplibre-gl/dist/sources/VectorTileSource.svelte
VectorTileSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/VectorTileSource.svelte";
function VectorTileSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, VectorTileSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "vector"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(VectorTileSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", VectorTileSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      VectorTileSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  VectorTileSource = hmr(VectorTileSource);
  import.meta.hot.accept((module) => {
    VectorTileSource[HMR].update(module.default);
  });
}
var VectorTileSource_default = VectorTileSource;

// node_modules/svelte-maplibre-gl/dist/sources/RasterTileSource.svelte
var import_maplibre_gl11 = __toESM(require_maplibre_gl());
RasterTileSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/RasterTileSource.svelte";
function RasterTileSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RasterTileSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "raster"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(RasterTileSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", RasterTileSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      RasterTileSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  RasterTileSource = hmr(RasterTileSource);
  import.meta.hot.accept((module) => {
    RasterTileSource[HMR].update(module.default);
  });
}
var RasterTileSource_default = RasterTileSource;

// node_modules/svelte-maplibre-gl/dist/sources/RasterDEMTileSource.svelte
var import_maplibre_gl12 = __toESM(require_maplibre_gl());
RasterDEMTileSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/RasterDEMTileSource.svelte";
function RasterDEMTileSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RasterDEMTileSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "raster-dem"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(RasterDEMTileSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", RasterDEMTileSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      RasterDEMTileSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  RasterDEMTileSource = hmr(RasterDEMTileSource);
  import.meta.hot.accept((module) => {
    RasterDEMTileSource[HMR].update(module.default);
  });
}
var RasterDEMTileSource_default = RasterDEMTileSource;

// node_modules/svelte-maplibre-gl/dist/sources/GeoJSONSource.svelte
var import_maplibre_gl13 = __toESM(require_maplibre_gl());
GeoJSONSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/GeoJSONSource.svelte";
function GeoJSONSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GeoJSONSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "geojson"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(GeoJSONSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", GeoJSONSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      GeoJSONSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  GeoJSONSource = hmr(GeoJSONSource);
  import.meta.hot.accept((module) => {
    GeoJSONSource[HMR].update(module.default);
  });
}
var GeoJSONSource_default = GeoJSONSource;

// node_modules/svelte-maplibre-gl/dist/sources/ImageSource.svelte
var import_maplibre_gl14 = __toESM(require_maplibre_gl());
ImageSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/ImageSource.svelte";
function ImageSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ImageSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "image"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(ImageSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", ImageSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      ImageSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  ImageSource = hmr(ImageSource);
  import.meta.hot.accept((module) => {
    ImageSource[HMR].update(module.default);
  });
}
var ImageSource_default = ImageSource;

// node_modules/svelte-maplibre-gl/dist/sources/VideoSource.svelte
var import_maplibre_gl15 = __toESM(require_maplibre_gl());
VideoSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/VideoSource.svelte";
function VideoSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, VideoSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "video"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(VideoSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", VideoSource, 18, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      VideoSource,
      17,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  VideoSource = hmr(VideoSource);
  import.meta.hot.accept((module) => {
    VideoSource[HMR].update(module.default);
  });
}
var VideoSource_default = VideoSource;

// node_modules/svelte-maplibre-gl/dist/sources/CanvasSource.svelte
var import_maplibre_gl16 = __toESM(require_maplibre_gl());
CanvasSource[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/CanvasSource.svelte";
function CanvasSource($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, CanvasSource);
  var $$ownership_validator = create_ownership_validator($$props);
  let source = prop($$props, "source", 15, void 0), spec = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "source",
      "id",
      "children"
    ],
    "spec"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    $$ownership_validator.binding("source", RawSource_default, source);
    add_svelte_meta(
      () => RawSource_default(node, spread_props(
        {
          get id() {
            return $$props.id;
          },
          type: "canvas"
        },
        () => spec,
        {
          get source() {
            return source();
          },
          set source($$value) {
            source($$value);
          },
          children: wrap_snippet(CanvasSource, ($$anchor2, $$slotProps) => {
            var fragment_1 = comment();
            var node_1 = first_child(fragment_1);
            add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", CanvasSource, 17, 1);
            append($$anchor2, fragment_1);
          }),
          $$slots: { default: true }
        }
      )),
      "component",
      CanvasSource,
      16,
      0,
      { componentTag: "RawSource" }
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  CanvasSource = hmr(CanvasSource);
  import.meta.hot.accept((module) => {
    CanvasSource[HMR].update(module.default);
  });
}
var CanvasSource_default = CanvasSource;

// node_modules/svelte-maplibre-gl/dist/sources/FeatureState.svelte
var import_maplibre_gl17 = __toESM(require_maplibre_gl());
FeatureState[FILENAME] = "node_modules/svelte-maplibre-gl/dist/sources/FeatureState.svelte";
function FeatureState($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FeatureState);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  const source = $$props.source || getSourceContext().id;
  let prevKeys = [];
  let prevIdentifier = snapshot({ source, sourceLayer: $$props.sourceLayer, id: $$props.id });
  user_effect(() => {
    if (!mapCtx.map) {
      return;
    }
    const newKeys = new Set(Object.keys($$props.state));
    if (strict_equals(prevIdentifier.id, void 0, false)) {
      const targetIdChanged = strict_equals(prevIdentifier.id, $$props.id, false) || strict_equals(prevIdentifier.source, source, false) || strict_equals(prevIdentifier.sourceLayer, $$props.sourceLayer, false);
      for (const key of prevKeys) {
        if (targetIdChanged || !newKeys.has(key)) {
          mapCtx.map.removeFeatureState(prevIdentifier, key);
        }
      }
    }
    const identifier = snapshot({ source, sourceLayer: $$props.sourceLayer, id: $$props.id });
    if (strict_equals(identifier.id, void 0, false)) {
      mapCtx.map.setFeatureState(identifier, snapshot($$props.state));
    }
    prevKeys = Array.from(newKeys);
    prevIdentifier = identifier;
  });
  onDestroy(() => {
    if (mapCtx.map) {
      for (const key of prevKeys) {
        mapCtx.map.removeFeatureState(prevIdentifier, key);
      }
    }
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(() => snippet(node, () => $$props.children ?? noop), "render", FeatureState, 61, 0);
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  FeatureState = hmr(FeatureState);
  import.meta.hot.accept((module) => {
    FeatureState[HMR].update(module.default);
  });
}
var FeatureState_default = FeatureState;

// node_modules/svelte-maplibre-gl/dist/layers/FillLayer.svelte
var import_maplibre_gl19 = __toESM(require_maplibre_gl());

// node_modules/svelte-maplibre-gl/dist/layers/RawLayer.svelte
var import_maplibre_gl18 = __toESM(require_maplibre_gl());
RawLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/RawLayer.svelte";
function RawLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RawLayer);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  const id = $$props.id ?? generateLayerID();
  const layerCtx = prepareLayerContext();
  layerCtx.id = id;
  const addLayerObj = {
    id,
    type: $$props.type,
    layout: snapshot($$props.layout) ?? {},
    paint: snapshot($$props.paint) ?? {}
  };
  if (strict_equals(addLayerObj.type, "background", false)) {
    addLayerObj.source = $$props.source ?? getSourceContext().id;
  }
  if (strict_equals($$props.maxzoom, void 0, false)) {
    addLayerObj.maxzoom = $$props.maxzoom;
  }
  if (strict_equals($$props.minzoom, void 0, false)) {
    addLayerObj.minzoom = $$props.minzoom;
  }
  if (strict_equals($$props.metadata, void 0, false)) {
    addLayerObj.metadata = $$props.metadata;
  }
  if (strict_equals(addLayerObj.type, "background", false)) {
    if ($$props["source-layer"]) {
      addLayerObj["source-layer"] = $$props["source-layer"];
    }
    if ($$props.filter) {
      addLayerObj.filter = snapshot($$props.filter);
    }
  }
  let firstRun = true;
  mapCtx.waitForStyleLoaded(() => {
    mapCtx.addLayer(addLayerObj, $$props.beforeId);
  });
  user_effect(() => resetLayerEventListener(mapCtx.map, "click", id, $$props.onclick));
  user_effect(() => resetLayerEventListener(mapCtx.map, "dblclick", id, $$props.ondblclick));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mousedown", id, $$props.onmousedown));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseup", id, $$props.onmouseup));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mousemove", id, $$props.onmousemove));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseenter", id, $$props.onmouseenter));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseleave", id, $$props.onmouseleave));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseover", id, $$props.onmouseover));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseout", id, $$props.onmouseout));
  user_effect(() => resetLayerEventListener(mapCtx.map, "contextmenu", id, $$props.oncontextmenu));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchstart", id, $$props.ontouchstart));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchend", id, $$props.ontouchend));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchcancel", id, $$props.ontouchcancel));
  let prevPaint = snapshot($$props.paint) ?? {};
  user_effect(() => {
    $$props.paint;
    if (!firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        const keysRemoved = new Set(Object.keys(prevPaint));
        const _paint = snapshot($$props.paint) ?? {};
        for (const [key, value] of Object.entries(_paint)) {
          keysRemoved.delete(key);
          if (strict_equals(prevPaint[key], value, false)) {
            map.setPaintProperty(id, key, value);
          }
        }
        for (const key of keysRemoved) {
          map.setPaintProperty(id, key, void 0);
        }
        prevPaint = _paint;
      });
    }
  });
  let prevLayout = snapshot($$props.layout) ?? {};
  user_effect(() => {
    $$props.layout;
    if (!firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        const keysRemoved = new Set(Object.keys(prevLayout));
        const _layout = snapshot($$props.layout) ?? {};
        for (const [key, value] of Object.entries(_layout)) {
          keysRemoved.delete(key);
          if (strict_equals(prevLayout[key], value, false)) {
            map.setLayoutProperty(id, key, value);
          }
        }
        for (const key of keysRemoved) {
          map.setLayoutProperty(id, key, void 0);
        }
        prevLayout = _layout;
      });
    }
  });
  user_effect(() => {
    if ((strict_equals($$props.minzoom, void 0, false) || strict_equals($$props.maxzoom, void 0, false)) && !firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        map.setLayerZoomRange(id, $$props.minzoom ?? 0, $$props.maxzoom ?? 22);
      });
    }
  });
  user_effect(() => {
    $$props.filter;
    if (!firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        map.setFilter(id, snapshot($$props.filter));
      });
    }
  });
  user_effect(() => {
    if ($$props.beforeId && !firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        map.moveLayer(id, $$props.beforeId);
      });
    }
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    mapCtx.removeLayer(id);
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(() => snippet(node, () => $$props.children ?? noop), "render", RawLayer, 183, 0);
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  RawLayer = hmr(RawLayer);
  import.meta.hot.accept((module) => {
    RawLayer[HMR].update(module.default);
  });
}
var RawLayer_default = RawLayer;

// node_modules/svelte-maplibre-gl/dist/layers/FillLayer.svelte
FillLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/FillLayer.svelte";
function FillLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FillLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "fill",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(FillLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", FillLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    FillLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  FillLayer = hmr(FillLayer);
  import.meta.hot.accept((module) => {
    FillLayer[HMR].update(module.default);
  });
}
var FillLayer_default = FillLayer;

// node_modules/svelte-maplibre-gl/dist/layers/LineLayer.svelte
var import_maplibre_gl20 = __toESM(require_maplibre_gl());
LineLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/LineLayer.svelte";
function LineLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, LineLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "line",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(LineLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", LineLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    LineLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  LineLayer = hmr(LineLayer);
  import.meta.hot.accept((module) => {
    LineLayer[HMR].update(module.default);
  });
}
var LineLayer_default = LineLayer;

// node_modules/svelte-maplibre-gl/dist/layers/CircleLayer.svelte
var import_maplibre_gl21 = __toESM(require_maplibre_gl());
CircleLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/CircleLayer.svelte";
function CircleLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, CircleLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "circle",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(CircleLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", CircleLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    CircleLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  CircleLayer = hmr(CircleLayer);
  import.meta.hot.accept((module) => {
    CircleLayer[HMR].update(module.default);
  });
}
var CircleLayer_default = CircleLayer;

// node_modules/svelte-maplibre-gl/dist/layers/FillExtrusionLayer.svelte
var import_maplibre_gl22 = __toESM(require_maplibre_gl());
FillExtrusionLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/FillExtrusionLayer.svelte";
function FillExtrusionLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FillExtrusionLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "fill-extrusion",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(FillExtrusionLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", FillExtrusionLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    FillExtrusionLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  FillExtrusionLayer = hmr(FillExtrusionLayer);
  import.meta.hot.accept((module) => {
    FillExtrusionLayer[HMR].update(module.default);
  });
}
var FillExtrusionLayer_default = FillExtrusionLayer;

// node_modules/svelte-maplibre-gl/dist/layers/SymbolLayer.svelte
var import_maplibre_gl23 = __toESM(require_maplibre_gl());
SymbolLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/SymbolLayer.svelte";
function SymbolLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, SymbolLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "symbol",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(SymbolLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", SymbolLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    SymbolLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  SymbolLayer = hmr(SymbolLayer);
  import.meta.hot.accept((module) => {
    SymbolLayer[HMR].update(module.default);
  });
}
var SymbolLayer_default = SymbolLayer;

// node_modules/svelte-maplibre-gl/dist/layers/HeatmapLayer.svelte
var import_maplibre_gl24 = __toESM(require_maplibre_gl());
HeatmapLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/HeatmapLayer.svelte";
function HeatmapLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, HeatmapLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "heatmap",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(HeatmapLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", HeatmapLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    HeatmapLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  HeatmapLayer = hmr(HeatmapLayer);
  import.meta.hot.accept((module) => {
    HeatmapLayer[HMR].update(module.default);
  });
}
var HeatmapLayer_default = HeatmapLayer;

// node_modules/svelte-maplibre-gl/dist/layers/RasterLayer.svelte
var import_maplibre_gl25 = __toESM(require_maplibre_gl());
RasterLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/RasterLayer.svelte";
function RasterLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RasterLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "raster",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(RasterLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", RasterLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    RasterLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  RasterLayer = hmr(RasterLayer);
  import.meta.hot.accept((module) => {
    RasterLayer[HMR].update(module.default);
  });
}
var RasterLayer_default = RasterLayer;

// node_modules/svelte-maplibre-gl/dist/layers/HillshadeLayer.svelte
var import_maplibre_gl26 = __toESM(require_maplibre_gl());
HillshadeLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/HillshadeLayer.svelte";
function HillshadeLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, HillshadeLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "hillshade",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(HillshadeLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", HillshadeLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    HillshadeLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  HillshadeLayer = hmr(HillshadeLayer);
  import.meta.hot.accept((module) => {
    HillshadeLayer[HMR].update(module.default);
  });
}
var HillshadeLayer_default = HillshadeLayer;

// node_modules/svelte-maplibre-gl/dist/layers/ColorReliefLayer.svelte
var import_maplibre_gl27 = __toESM(require_maplibre_gl());
ColorReliefLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/ColorReliefLayer.svelte";
function ColorReliefLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ColorReliefLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children", "sourceLayer"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props(
      {
        type: "color-relief",
        get "source-layer"() {
          return $$props.sourceLayer;
        }
      },
      () => props,
      {
        children: wrap_snippet(ColorReliefLayer, ($$anchor2, $$slotProps) => {
          var fragment_1 = comment();
          var node_1 = first_child(fragment_1);
          add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", ColorReliefLayer, 23, 1);
          append($$anchor2, fragment_1);
        }),
        $$slots: { default: true }
      }
    )),
    "component",
    ColorReliefLayer,
    22,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  ColorReliefLayer = hmr(ColorReliefLayer);
  import.meta.hot.accept((module) => {
    ColorReliefLayer[HMR].update(module.default);
  });
}
var ColorReliefLayer_default = ColorReliefLayer;

// node_modules/svelte-maplibre-gl/dist/layers/BackgroundLayer.svelte
var import_maplibre_gl28 = __toESM(require_maplibre_gl());
BackgroundLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/BackgroundLayer.svelte";
function BackgroundLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, BackgroundLayer);
  let props = rest_props($$props, ["$$slots", "$$events", "$$legacy", "children"], "props");
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => RawLayer_default(node, spread_props({ type: "background" }, () => props, {
      children: wrap_snippet(BackgroundLayer, ($$anchor2, $$slotProps) => {
        var fragment_1 = comment();
        var node_1 = first_child(fragment_1);
        add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", BackgroundLayer, 19, 1);
        append($$anchor2, fragment_1);
      }),
      $$slots: { default: true }
    })),
    "component",
    BackgroundLayer,
    18,
    0,
    { componentTag: "RawLayer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  BackgroundLayer = hmr(BackgroundLayer);
  import.meta.hot.accept((module) => {
    BackgroundLayer[HMR].update(module.default);
  });
}
var BackgroundLayer_default = BackgroundLayer;

// node_modules/svelte-maplibre-gl/dist/layers/CustomLayer.svelte
var import_maplibre_gl29 = __toESM(require_maplibre_gl());
CustomLayer[FILENAME] = "node_modules/svelte-maplibre-gl/dist/layers/CustomLayer.svelte";
function CustomLayer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, CustomLayer);
  var $$ownership_validator = create_ownership_validator($$props);
  let implementation = prop($$props, "implementation", 7);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  const id = $$props.id ?? generateLayerID();
  $$ownership_validator.mutation("implementation", ["implementation", "id"], implementation().id ??= id, 45, 2);
  $$ownership_validator.mutation("implementation", ["implementation", "type"], implementation().type = "custom", 46, 2);
  let firstRun = true;
  mapCtx.waitForStyleLoaded(() => {
    mapCtx.addLayer(implementation(), $$props.beforeId);
  });
  user_effect(() => resetLayerEventListener(mapCtx.map, "click", id, $$props.onclick));
  user_effect(() => resetLayerEventListener(mapCtx.map, "dblclick", id, $$props.ondblclick));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mousedown", id, $$props.onmousedown));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseup", id, $$props.onmouseup));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mousemove", id, $$props.onmousemove));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseenter", id, $$props.onmouseenter));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseleave", id, $$props.onmouseleave));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseover", id, $$props.onmouseover));
  user_effect(() => resetLayerEventListener(mapCtx.map, "mouseout", id, $$props.onmouseout));
  user_effect(() => resetLayerEventListener(mapCtx.map, "contextmenu", id, $$props.oncontextmenu));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchstart", id, $$props.ontouchstart));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchend", id, $$props.ontouchend));
  user_effect(() => resetLayerEventListener(mapCtx.map, "touchcancel", id, $$props.ontouchcancel));
  user_effect(() => {
    if ($$props.beforeId && !firstRun) {
      mapCtx.waitForStyleLoaded((map) => {
        map.moveLayer(id, $$props.beforeId);
      });
    }
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    mapCtx.removeLayer(id);
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(() => snippet(node, () => $$props.children ?? noop), "render", CustomLayer, 84, 0);
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  CustomLayer = hmr(CustomLayer);
  import.meta.hot.accept((module) => {
    CustomLayer[HMR].update(module.default);
  });
}
var CustomLayer_default = CustomLayer;

// node_modules/svelte-maplibre-gl/dist/markers/Marker.svelte
var import_maplibre_gl30 = __toESM(require_maplibre_gl());
Marker[FILENAME] = "node_modules/svelte-maplibre-gl/dist/markers/Marker.svelte";
var root_1 = add_locations(from_html(`<div><!></div>`), Marker[FILENAME], [[169, 1]]);
var root2 = add_locations(from_html(`<!> <!>`, 1), Marker[FILENAME], []);
function Marker($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Marker);
  let container = tag(state(null), "container");
  let lnglat = prop($$props, "lnglat", 15), className = prop($$props, "class", 3, void 0), restOptions = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "lnglat",
      "class",
      "draggable",
      "rotation",
      "rotationAlignment",
      "pitchAlignment",
      "opacity",
      "color",
      "opacityWhenCovered",
      "offset",
      "subpixelPositioning",
      "content",
      "children",
      "ondrag",
      "ondragstart",
      "ondragend"
    ],
    "restOptions"
  );
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let marker = tag(state(null), "marker");
  const markerCtx = prepareMarkerContext();
  user_effect(() => {
    if (get(marker)) {
      return;
    }
    if (!mapCtx.map) throw new Error("MapLibre is not initialized");
    const options = {
      draggable: $$props.draggable,
      offset: $$props.offset,
      opacity: $$props.opacity,
      className: className(),
      opacityWhenCovered: $$props.opacityWhenCovered,
      rotation: $$props.rotation,
      color: $$props.color,
      rotationAlignment: $$props.rotationAlignment,
      pitchAlignment: $$props.pitchAlignment,
      subpixelPositioning: $$props.subpixelPositioning,
      ...restOptions
    };
    if ($$props.content) {
      if (!get(container)) throw new Error("Marker container is not initialized");
      options.element = get(container);
    }
    set(marker, new import_maplibre_gl30.default.Marker(options));
    markerCtx.marker = get(marker);
    get(marker).setLngLat(snapshot(lnglat())).addTo(mapCtx.map);
    get(marker).on("drag", (e) => {
      if (get(marker)) {
        lnglat(formatLngLat(lnglat(), get(marker).getLngLat()));
      }
      $$props.ondrag?.(e);
    });
  });
  let firstRun = true;
  user_effect(() => resetEventListener(get(marker), "dragstart", $$props.ondragstart));
  user_effect(() => resetEventListener(get(marker), "dragend", $$props.ondragend));
  user_effect(() => {
    $$props.draggable;
    if (!firstRun) {
      get(marker)?.setDraggable($$props.draggable);
    }
  });
  user_effect(() => {
    if (lnglat() && !firstRun) {
      get(marker)?.setLngLat(lnglat());
    }
  });
  user_effect(() => {
    $$props.rotation;
    if (!firstRun) {
      get(marker)?.setRotation($$props.rotation);
    }
  });
  user_effect(() => {
    if ($$props.offset && !firstRun) {
      get(marker)?.setOffset($$props.offset);
    }
  });
  user_effect(() => {
    $$props.opacity;
    $$props.opacityWhenCovered;
    if (!firstRun) {
      get(marker)?.setOpacity($$props.opacity, $$props.opacityWhenCovered);
    }
  });
  user_effect(() => {
    if ($$props.rotationAlignment && !firstRun) {
      get(marker)?.setRotationAlignment($$props.rotationAlignment);
    }
  });
  user_effect(() => {
    if ($$props.pitchAlignment && !firstRun) {
      get(marker)?.setPitchAlignment($$props.pitchAlignment);
    }
  });
  user_effect(() => {
    if (strict_equals($$props.subpixelPositioning, void 0, false) && !firstRun) {
      get(marker)?.setSubpixelPositioning($$props.subpixelPositioning);
    }
  });
  user_effect(() => {
    const classNames = (className() ?? "")?.split(/\s/).filter(Boolean);
    if (get(marker) && !firstRun) {
      for (const className2 of classNames) {
        get(marker).addClassName(className2);
      }
    }
    return () => {
      if (get(marker)) {
        for (const className2 of classNames) {
          get(marker).removeClassName(className2);
        }
      }
    };
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    get(marker)?.remove();
  });
  var $$exports = { ...legacy_api() };
  var fragment = root2();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var div = root_1();
      var node_1 = child(div);
      add_svelte_meta(() => snippet(node_1, () => $$props.content), "render", Marker, 170, 2);
      reset(div);
      bind_this(div, ($$value) => set(container, $$value), () => get(container));
      append($$anchor2, div);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.content) $$render(consequent);
      }),
      "if",
      Marker,
      168,
      0
    );
  }
  var node_2 = sibling(node, 2);
  add_svelte_meta(() => snippet(node_2, () => $$props.children ?? noop), "render", Marker, 174, 0);
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  Marker = hmr(Marker);
  import.meta.hot.accept((module) => {
    Marker[HMR].update(module.default);
  });
}
var Marker_default = Marker;

// node_modules/svelte-maplibre-gl/dist/markers/Popup.svelte
var import_maplibre_gl31 = __toESM(require_maplibre_gl());
Popup[FILENAME] = "node_modules/svelte-maplibre-gl/dist/markers/Popup.svelte";
var root3 = add_locations(from_html(`<div><!></div>`), Popup[FILENAME], [[183, 0]]);
function Popup($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, Popup);
  let className = prop($$props, "class", 3, void 0), open = prop($$props, "open", 15), restOptions = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "lnglat",
      "class",
      "open",
      "offset",
      "maxWidth",
      "subpixelPositioning",
      "closeButton",
      "closeOnClick",
      "closeOnMove",
      "focusAfterOpen",
      "onopen",
      "onclose",
      "children"
    ],
    "restOptions"
  );
  let container = tag(state(null), "container");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let popup = tag(state(null), "popup");
  const markerContext = getMarkerContext();
  user_effect(() => {
    if (get(popup)) {
      return;
    }
    if (!mapCtx.map) throw new Error("MapLibre is not initialized");
    const options = {
      className: className(),
      offset: $$props.offset,
      subpixelPositioning: $$props.subpixelPositioning,
      ...restOptions
    };
    strict_equals($$props.maxWidth, void 0, false) && assign(options, "maxWidth", $$props.maxWidth, "node_modules/​svelte-maplibre-gl/​dist/​markers/​Popup.svelte:62:29");
    strict_equals($$props.closeButton, void 0, false) && assign(options, "closeButton", $$props.closeButton, "node_modules/​svelte-maplibre-gl/​dist/​markers/​Popup.svelte:63:32");
    strict_equals($$props.closeOnClick, void 0, false) && assign(options, "closeOnClick", $$props.closeOnClick, "node_modules/​svelte-maplibre-gl/​dist/​markers/​Popup.svelte:64:33");
    strict_equals($$props.closeOnMove, void 0, false) && assign(options, "closeOnMove", $$props.closeOnMove, "node_modules/​svelte-maplibre-gl/​dist/​markers/​Popup.svelte:65:32");
    strict_equals($$props.focusAfterOpen, void 0, false) && assign(options, "focusAfterOpen", $$props.focusAfterOpen, "node_modules/​svelte-maplibre-gl/​dist/​markers/​Popup.svelte:66:35");
    if (!get(container)) throw new Error("Popup content container is not initialized");
    set(popup, new import_maplibre_gl31.default.Popup(options));
    get(popup).setDOMContent(get(container));
    if ($$props.lnglat) {
      get(popup).setLngLat($$props.lnglat);
      if (strict_equals(open(), void 0)) {
        open(true);
      }
    }
  });
  user_effect(() => {
    if (markerContext?.marker) {
      markerContext.marker.setPopup(get(popup));
    }
  });
  let firstRun = true;
  let internalOpen = false;
  user_effect(() => {
    if (strict_equals(open(), true)) {
      if (mapCtx.map && !internalOpen) {
        if (!get(popup)?.getLngLat()) {
          const lnglat = markerContext?.marker?.getLngLat();
          if (!lnglat) return;
          get(popup)?.setLngLat(lnglat);
        }
        get(popup)?.addTo(mapCtx.map);
        internalOpen = true;
      }
    } else if (strict_equals(open(), false) && internalOpen) {
      get(popup)?.remove();
      internalOpen = false;
    }
  });
  user_effect(() => {
    function _onopen(ev) {
      open(true);
      internalOpen = true;
      $$props.onopen?.(ev);
    }
    get(popup)?.on("open", _onopen);
    return () => {
      get(popup)?.off("open", _onopen);
    };
  });
  user_effect(() => {
    function _onclose(ev) {
      open(false);
      internalOpen = false;
      $$props.onclose?.(ev);
    }
    get(popup)?.on("close", _onclose);
    return () => {
      get(popup)?.off("close", _onclose);
    };
  });
  user_effect(() => {
    if ($$props.lnglat && !firstRun) {
      get(popup)?.setLngLat($$props.lnglat);
    }
  });
  user_effect(() => {
    $$props.maxWidth;
    if (!firstRun) {
      get(popup)?.setMaxWidth($$props.maxWidth ?? "none");
    }
  });
  user_effect(() => {
    if ($$props.offset && !firstRun) {
      get(popup)?.setOffset($$props.offset);
    }
  });
  user_effect(() => {
    if (strict_equals($$props.subpixelPositioning, void 0, false) && !firstRun) {
      get(popup)?.setSubpixelPositioning($$props.subpixelPositioning);
    }
  });
  user_effect(() => {
    const classNames = (className() ?? "")?.split(/\s/).filter(Boolean);
    if (get(popup) && !firstRun) {
      for (const className2 of classNames) {
        get(popup).addClassName(className2);
      }
    }
    return () => {
      if (get(popup)) {
        for (const className2 of classNames) {
          get(popup).removeClassName(className2);
        }
      }
    };
  });
  user_effect(() => {
    firstRun = false;
  });
  onDestroy(() => {
    get(popup)?.remove();
    markerContext?.marker?.setPopup(null);
  });
  var $$exports = { ...legacy_api() };
  var div = root3();
  var node = child(div);
  add_svelte_meta(() => snippet(node, () => $$props.children ?? noop), "render", Popup, 184, 1);
  reset(div);
  bind_this(div, ($$value) => set(container, $$value), () => get(container));
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  Popup = hmr(Popup);
  import.meta.hot.accept((module) => {
    Popup[HMR].update(module.default);
  });
}
var Popup_default = Popup;

// node_modules/svelte-maplibre-gl/dist/controls/AttributionControl.svelte
var import_maplibre_gl32 = __toESM(require_maplibre_gl());
AttributionControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/AttributionControl.svelte";
function AttributionControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, AttributionControl);
  let options = rest_props($$props, ["$$slots", "$$events", "$$legacy", "position"], "options");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl32.default.AttributionControl(snapshot(options));
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  AttributionControl = hmr(AttributionControl);
  import.meta.hot.accept((module) => {
    AttributionControl[HMR].update(module.default);
  });
}
var AttributionControl_default = AttributionControl;

// node_modules/svelte-maplibre-gl/dist/controls/GeolocateControl.svelte
var import_maplibre_gl33 = __toESM(require_maplibre_gl());
GeolocateControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/GeolocateControl.svelte";
function GeolocateControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GeolocateControl);
  let control = prop($$props, "control", 15), autoTrigger = prop($$props, "autoTrigger", 3, false), options = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "position",
      "control",
      "autoTrigger",
      "ontrackuserlocationend",
      "ontrackuserlocationstart",
      "onuserlocationlostfocus",
      "onuserlocationfocus",
      "ongeolocate",
      "onerror",
      "onoutofmaxbounds"
    ],
    "options"
  );
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  user_effect(() => {
    untrack(() => control() && mapCtx.map?.removeControl(control()));
    control(new import_maplibre_gl33.default.GeolocateControl(options));
    mapCtx.map?.addControl(untrack(() => control()), $$props.position);
  });
  user_effect(() => {
    if (autoTrigger()) {
      if (mapCtx.map?.loaded()) {
        control()?.trigger();
      } else {
        mapCtx.map?.once("load", () => {
          control()?.trigger();
        });
      }
    }
  });
  user_effect(() => resetEventListener(control(), "trackuserlocationstart", $$props.ontrackuserlocationstart));
  user_effect(() => resetEventListener(control(), "trackuserlocationend", $$props.ontrackuserlocationend));
  user_effect(() => resetEventListener(control(), "userlocationlostfocus", $$props.onuserlocationlostfocus));
  user_effect(() => resetEventListener(control(), "userlocationfocus", $$props.onuserlocationfocus));
  user_effect(() => resetEventListener(control(), "geolocate", $$props.ongeolocate));
  user_effect(() => resetEventListener(control(), "error", $$props.onerror));
  user_effect(() => resetEventListener(control(), "outofmaxbounds", $$props.onoutofmaxbounds));
  onDestroy(() => {
    if (control()) {
      mapCtx.map?.removeControl(control());
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  GeolocateControl = hmr(GeolocateControl);
  import.meta.hot.accept((module) => {
    GeolocateControl[HMR].update(module.default);
  });
}
var GeolocateControl_default = GeolocateControl;

// node_modules/svelte-maplibre-gl/dist/controls/GlobeControl.svelte
var import_maplibre_gl34 = __toESM(require_maplibre_gl());
GlobeControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/GlobeControl.svelte";
function GlobeControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GlobeControl);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl34.default.GlobeControl();
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  GlobeControl = hmr(GlobeControl);
  import.meta.hot.accept((module) => {
    GlobeControl[HMR].update(module.default);
  });
}
var GlobeControl_default = GlobeControl;

// node_modules/svelte-maplibre-gl/dist/controls/NavigationControl.svelte
var import_maplibre_gl35 = __toESM(require_maplibre_gl());
NavigationControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/NavigationControl.svelte";
function NavigationControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, NavigationControl);
  let options = rest_props($$props, ["$$slots", "$$events", "$$legacy", "position"], "options");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl35.default.NavigationControl(snapshot(options));
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  NavigationControl = hmr(NavigationControl);
  import.meta.hot.accept((module) => {
    NavigationControl[HMR].update(module.default);
  });
}
var NavigationControl_default = NavigationControl;

// node_modules/svelte-maplibre-gl/dist/controls/FullScreenControl.svelte
var import_maplibre_gl36 = __toESM(require_maplibre_gl());
FullScreenControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/FullScreenControl.svelte";
function FullScreenControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FullScreenControl);
  let options = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "position",
      "onfullscreenstart",
      "onfullscreenend"
    ],
    "options"
  );
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl36.default.FullscreenControl(options);
    mapCtx.map?.addControl(control, $$props.position);
  });
  user_effect(() => resetEventListener(control, "fullscreenstart", $$props.onfullscreenstart));
  user_effect(() => resetEventListener(control, "fullscreenend", $$props.onfullscreenend));
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  FullScreenControl = hmr(FullScreenControl);
  import.meta.hot.accept((module) => {
    FullScreenControl[HMR].update(module.default);
  });
}
var FullScreenControl_default = FullScreenControl;

// node_modules/svelte-maplibre-gl/dist/controls/TerrainControl.svelte
var import_maplibre_gl37 = __toESM(require_maplibre_gl());
TerrainControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/TerrainControl.svelte";
function TerrainControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TerrainControl);
  let restOptions = rest_props($$props, ["$$slots", "$$events", "$$legacy", "position", "source"], "restOptions");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  const options = tag(
    user_derived(() => ({
      source: $$props.source ?? getSourceContext().id,
      ...restOptions
    })),
    "options"
  );
  let control = null;
  user_effect(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
    control = new import_maplibre_gl37.default.TerrainControl(snapshot(get(options)));
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  TerrainControl = hmr(TerrainControl);
  import.meta.hot.accept((module) => {
    TerrainControl[HMR].update(module.default);
  });
}
var TerrainControl_default = TerrainControl;

// node_modules/svelte-maplibre-gl/dist/controls/ScaleControl.svelte
var import_maplibre_gl38 = __toESM(require_maplibre_gl());
ScaleControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/ScaleControl.svelte";
function ScaleControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ScaleControl);
  let options = rest_props($$props, ["$$slots", "$$events", "$$legacy", "position"], "options");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl38.default.ScaleControl(snapshot(options));
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  ScaleControl = hmr(ScaleControl);
  import.meta.hot.accept((module) => {
    ScaleControl[HMR].update(module.default);
  });
}
var ScaleControl_default = ScaleControl;

// node_modules/svelte-maplibre-gl/dist/controls/LogoControl.svelte
var import_maplibre_gl39 = __toESM(require_maplibre_gl());
LogoControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/LogoControl.svelte";
function LogoControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, LogoControl);
  let options = rest_props($$props, ["$$slots", "$$events", "$$legacy", "position"], "options");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let control = null;
  user_effect(() => {
    control && mapCtx.map?.removeControl(control);
    control = new import_maplibre_gl39.default.LogoControl(snapshot(options));
    mapCtx.map?.addControl(control, $$props.position);
  });
  onDestroy(() => {
    if (control) {
      mapCtx.map?.removeControl(control);
    }
  });
  var $$exports = { ...legacy_api() };
  return pop($$exports);
}
if (import.meta.hot) {
  LogoControl = hmr(LogoControl);
  import.meta.hot.accept((module) => {
    LogoControl[HMR].update(module.default);
  });
}
var LogoControl_default = LogoControl;

// node_modules/svelte-maplibre-gl/dist/controls/CustomControl.svelte
var import_maplibre_gl40 = __toESM(require_maplibre_gl());
CustomControl[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/CustomControl.svelte";
var root_12 = add_locations(from_html(`<div><!></div>`), CustomControl[FILENAME], [[49, 1]]);
function CustomControl($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, CustomControl);
  let group = prop($$props, "group", 3, true);
  if (!$$props.control && !$$props.children) throw new Error("You must provide either control or children.");
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let el = tag(state(void 0), "el");
  let control = tag(
    user_derived(() => {
      if ($$props.control) {
        return $$props.control;
      }
      return {
        onAdd: () => {
          return get(el);
        },
        onRemove: () => {
          get(el)?.parentNode?.removeChild(get(el));
        }
      };
    }),
    "control"
  );
  user_effect(() => {
    if (get(control)) {
      mapCtx.map?.addControl(get(control), $$props.position);
    }
    return () => {
      get(control) && mapCtx.map?.removeControl(get(control));
    };
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var div = root_12();
      let classes;
      var node_1 = child(div);
      add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", CustomControl, 50, 2);
      reset(div);
      bind_this(div, ($$value) => set(el, $$value), () => get(el));
      template_effect(() => classes = set_class(div, 1, `maplibregl-ctrl ${$$props.class}`, null, classes, { "maplibregl-ctrl-group": group() }));
      append($$anchor2, div);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (!$$props.control) $$render(consequent);
      }),
      "if",
      CustomControl,
      48,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  CustomControl = hmr(CustomControl);
  import.meta.hot.accept((module) => {
    CustomControl[HMR].update(module.default);
  });
}
var CustomControl_default = CustomControl;

// node_modules/svelte-maplibre-gl/dist/controls/Hash.svelte
var import_maplibre_gl41 = __toESM(require_maplibre_gl());
Hash[FILENAME] = "node_modules/svelte-maplibre-gl/dist/controls/Hash.svelte";
function Hash($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Hash);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let hash = new import_maplibre_gl41.default.Hash().addTo(mapCtx.map);
  if (!hash._onHashChange()) {
    hash._updateHash();
  }
  onDestroy(() => {
    hash.remove();
  });
  var $$exports = { ...legacy_api() };
  init();
  return pop($$exports);
}
if (import.meta.hot) {
  Hash = hmr(Hash);
  import.meta.hot.accept((module) => {
    Hash[HMR].update(module.default);
  });
}
var Hash_default = Hash;

// node_modules/svelte-maplibre-gl/dist/utilities/ImageLoader.svelte
var import_maplibre_gl42 = __toESM(require_maplibre_gl());
ImageLoader[FILENAME] = "node_modules/svelte-maplibre-gl/dist/utilities/ImageLoader.svelte";
function ImageLoader($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ImageLoader);
  const mapCtx = getMapContext();
  if (!mapCtx.map) throw new Error("Map instance is not initialized.");
  let loading = prop($$props, "loading", 15);
  let initialLoaded = tag(state(false), "initialLoaded");
  const loadedImages = /* @__PURE__ */ new Map();
  user_effect(() => {
    for (const [id, url] of loadedImages) {
      const src = $$props.images[id];
      if (src) {
        const newUrl = Array.isArray(src) ? src[0] : src;
        if (strict_equals(url, newUrl)) {
          continue;
        } else {
          loadedImages.delete(id);
        }
      } else {
        loadedImages.delete(id);
        mapCtx.map?.removeImage(id);
      }
    }
    const tasks = [];
    for (const [id, src] of Object.entries($$props.images)) {
      if (loadedImages.has(id)) {
        const image = mapCtx.map?.getImage(id);
        if (image) {
          const _src = snapshot(src);
          const options2 = Array.isArray(_src) ? _src[1] : {};
          let changed = false;
          if (strict_equals(image.pixelRatio, options2.pixelRatio ?? 1, false)) {
            image.pixelRatio = options2.pixelRatio ?? 1;
            changed = true;
          }
          if (strict_equals(image.sdf, options2.sdf ?? false, false)) {
            image.sdf = options2.sdf ?? false;
            changed = true;
          }
          if (strict_equals(image.stretchX, options2.stretchX, false)) {
            image.stretchX = options2.stretchX;
            changed = true;
          }
          if (strict_equals(image.stretchY, options2.stretchY, false)) {
            image.stretchY = options2.stretchY;
            changed = true;
          }
          if (strict_equals(image.content, options2.content, false)) {
            image.content = options2.content;
            changed = true;
          }
          if (strict_equals(image.textFitHeight, options2.textFitHeight, false)) {
            image.textFitHeight = options2.textFitHeight;
            changed = true;
          }
          if (strict_equals(image.textFitWidth, options2.textFitWidth, false)) {
            image.textFitWidth = options2.textFitWidth;
            changed = true;
          }
          if (changed) {
            mapCtx.map?.style.updateImage(id, image);
          }
        }
        continue;
      }
      const [url, options] = Array.isArray(src) ? src : [src, void 0];
      loadedImages.set(id, url);
      tasks.push((async () => {
        const image = (await track_reactivity_loss(mapCtx.map?.loadImage(url)))();
        if (mapCtx.map?.getImage(id)) {
          mapCtx.map?.removeImage(id);
        }
        if (image && loadedImages.has(id)) {
          mapCtx.map?.addImage(id, image?.data, options);
        }
      })());
    }
    if (tasks.length > 0) {
      loading(true);
      Promise.allSettled([tasks]).then((results) => {
        const failures = results.filter((r) => strict_equals(r.status, "rejected"));
        if (failures.length > 0) {
          console.error(...log_if_contains_state("error", `${failures.length} images failed to load:`, failures));
        }
        set(initialLoaded, true);
        loading(false);
      });
    } else {
      set(initialLoaded, true);
    }
  });
  onDestroy(() => {
    for (const id of loadedImages.keys()) {
      mapCtx.map?.removeImage(id);
    }
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var fragment_1 = comment();
      var node_1 = first_child(fragment_1);
      add_svelte_meta(() => snippet(node_1, () => $$props.children ?? noop), "render", ImageLoader, 128, 1);
      append($$anchor2, fragment_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (get(initialLoaded)) $$render(consequent);
      }),
      "if",
      ImageLoader,
      127,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  ImageLoader = hmr(ImageLoader);
  import.meta.hot.accept((module) => {
    ImageLoader[HMR].update(module.default);
  });
}
var ImageLoader_default = ImageLoader;

// node_modules/svelte-maplibre-gl/dist/utilities/QuerySourceFeatures.svelte
var import_maplibre_gl43 = __toESM(require_maplibre_gl());
QuerySourceFeatures[FILENAME] = "node_modules/svelte-maplibre-gl/dist/utilities/QuerySourceFeatures.svelte";
function QuerySourceFeatures($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, QuerySourceFeatures);
  let features = prop($$props, "features", 31, () => tag_proxy(proxy([]), "features")), options = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "map",
      "source",
      "features",
      "children"
    ],
    "options"
  );
  features([]);
  let trigger = tag(state(0), "trigger");
  let map = tag(user_derived(() => $$props.map || getMapContext().map), "map");
  let source = tag(user_derived(() => $$props.source || getSourceContext().id), "source");
  user_effect(() => {
    get(map)?.on("render", update2);
    return () => {
      get(map)?.off("render", update2);
    };
  });
  function update2() {
    update(trigger);
  }
  user_effect(() => {
    get(trigger);
    if (!get(map) || !get(source)) {
      features([]);
      return;
    }
    let queriedFeature = get(map).querySourceFeatures(get(source), snapshot(options));
    let closed = /* @__PURE__ */ new Set();
    queriedFeature = queriedFeature.filter((feature) => {
      if (strict_equals(feature.id, void 0, false)) {
        if (closed.has(feature.id)) {
          return false;
        }
      }
      closed.add(feature.id);
      return true;
    });
    queriedFeature.sort((a, b) => {
      return String(a.id).localeCompare(String(b.id));
    });
    features(queriedFeature);
  });
  onDestroy(() => {
    features([]);
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var fragment_1 = comment();
      var node_1 = first_child(fragment_1);
      add_svelte_meta(
        () => each(node_1, 17, features, (feature) => feature.id, ($$anchor3, feature) => {
          var fragment_2 = comment();
          var node_2 = first_child(fragment_2);
          add_svelte_meta(() => snippet(node_2, () => $$props.children, () => get(feature)), "render", QuerySourceFeatures, 69, 54);
          append($$anchor3, fragment_2);
        }),
        "each",
        QuerySourceFeatures,
        69,
        14
      );
      append($$anchor2, fragment_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.children) $$render(consequent);
      }),
      "if",
      QuerySourceFeatures,
      69,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  QuerySourceFeatures = hmr(QuerySourceFeatures);
  import.meta.hot.accept((module) => {
    QuerySourceFeatures[HMR].update(module.default);
  });
}
var QuerySourceFeatures_default = QuerySourceFeatures;

// node_modules/svelte-maplibre-gl/dist/utilities/QueryRenderedFeatures.svelte
var import_maplibre_gl44 = __toESM(require_maplibre_gl());
QueryRenderedFeatures[FILENAME] = "node_modules/svelte-maplibre-gl/dist/utilities/QueryRenderedFeatures.svelte";
function QueryRenderedFeatures($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, QueryRenderedFeatures);
  let features = prop($$props, "features", 31, () => tag_proxy(proxy([]), "features")), options = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "map",
      "geometry",
      "features",
      "children",
      "layers"
    ],
    "options"
  );
  features([]);
  let trigger = tag(state(0), "trigger");
  let map = tag(user_derived(() => $$props.map || getMapContext().map), "map");
  user_effect(() => {
    get(map)?.on("render", update2);
    return () => {
      get(map)?.off("render", update2);
    };
  });
  function update2() {
    update(trigger);
  }
  user_effect(() => {
    get(trigger);
    if (!get(map)) {
      features([]);
      return;
    }
    let _options = {
      layers: $$props.layers || [getLayerContext()?.id || ""],
      ...options
    };
    let _geometry = $$props.geometry;
    let queriedFeatures = (_geometry ? get(map).queryRenderedFeatures(_geometry, _options) : get(map).queryRenderedFeatures(_options)).map((feature, index) => {
      feature.id = feature.id ?? feature.properties?.id ?? `svmlgl-feature-${index}`;
      return feature;
    });
    queriedFeatures.sort((a, b) => {
      return String(a.id).localeCompare(String(b.id));
    });
    features(queriedFeatures);
  });
  onDestroy(() => {
    features([]);
  });
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var fragment_1 = comment();
      var node_1 = first_child(fragment_1);
      add_svelte_meta(
        () => each(node_1, 17, features, (feature) => feature.id, ($$anchor3, feature) => {
          var fragment_2 = comment();
          var node_2 = first_child(fragment_2);
          add_svelte_meta(() => snippet(node_2, () => $$props.children, () => get(feature)), "render", QueryRenderedFeatures, 64, 54);
          append($$anchor3, fragment_2);
        }),
        "each",
        QueryRenderedFeatures,
        64,
        14
      );
      append($$anchor2, fragment_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.children) $$render(consequent);
      }),
      "if",
      QueryRenderedFeatures,
      64,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  QueryRenderedFeatures = hmr(QueryRenderedFeatures);
  import.meta.hot.accept((module) => {
    QueryRenderedFeatures[HMR].update(module.default);
  });
}
var QueryRenderedFeatures_default = QueryRenderedFeatures;
export {
  AttributionControl_default as AttributionControl,
  BackgroundLayer_default as BackgroundLayer,
  CanvasSource_default as CanvasSource,
  CircleLayer_default as CircleLayer,
  ColorReliefLayer_default as ColorReliefLayer,
  CustomControl_default as CustomControl,
  CustomLayer_default as CustomLayer,
  FeatureState_default as FeatureState,
  FillExtrusionLayer_default as FillExtrusionLayer,
  FillLayer_default as FillLayer,
  FullScreenControl_default as FullScreenControl,
  GeoJSONSource_default as GeoJSONSource,
  GeolocateControl_default as GeolocateControl,
  GlobeControl_default as GlobeControl,
  Hash_default as Hash,
  HeatmapLayer_default as HeatmapLayer,
  HillshadeLayer_default as HillshadeLayer,
  Image_default as Image,
  ImageLoader_default as ImageLoader,
  ImageSource_default as ImageSource,
  Light_default as Light,
  LineLayer_default as LineLayer,
  LogoControl_default as LogoControl,
  MapLibre_default as Map,
  MapLibre_default as MapLibre,
  Marker_default as Marker,
  NavigationControl_default as NavigationControl,
  Popup_default as Popup,
  Projection_default as Projection,
  Protocol_default as Protocol,
  QueryRenderedFeatures_default as QueryRenderedFeatures,
  QuerySourceFeatures_default as QuerySourceFeatures,
  RasterDEMTileSource_default as RasterDEMTileSource,
  RasterLayer_default as RasterLayer,
  RasterTileSource_default as RasterTileSource,
  RawLayer_default as RawLayer,
  RawSource_default as RawSource,
  ScaleControl_default as ScaleControl,
  Sky_default as Sky,
  Sprite_default as Sprite,
  SymbolLayer_default as SymbolLayer,
  Terrain_default as Terrain,
  TerrainControl_default as TerrainControl,
  VectorTileSource_default as VectorTileSource,
  VideoSource_default as VideoSource,
  getMapContext,
  getMarkerContext,
  getSourceContext
};
//# sourceMappingURL=svelte-maplibre-gl.js.map
